import "./Header.css";

export const Header = () => {
    return (
        <header className="header">
            <h1 className="title">Dashboard</h1>
            <nav className="nav">
                <ul className="list">
                    <li>Create New User</li>
                </ul>
            </nav>
        </header>
    );
};
